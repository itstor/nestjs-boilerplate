export const ConfigName = {
  APP: 'app-config',
  DB: 'db-config',
  JWT: 'jwt-config',
  THROTTLE: 'throttle-config',
  SMTP: 'smtp-config',
  KEY: 'key-config',
  BULL: 'bull-config',
  SENTRY: 'sentry-config',
} as const;
